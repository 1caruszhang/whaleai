# GEO 渠道发现与分发计划

Ticket 12 只创建可编辑、可确认的推荐与文章分配计划。它不创建订单、不扣费、不上传正文、不发布，也不创建 `PublishScheduler` wake。后续付费和发布只能消费已确认计划，并再次经过独立显式确认。

## Owner 与数据流

- Renderer 是当前 Chat Tab 的控制面。它经 `useTabApi().apiPost` 展示 Rust 派生的上下文、真实资源候选、证据与召回路命中；不直接请求 Provider 或 Management API。
- Node `DistributionPlanningService` 拥有 js_ai `dev` 四路召回、硬过滤、20/10 配额、文章映射和 30 分钟只读资源缓存。相同资源 kind 的并发加载合并为一次请求；Sidecar generation 更换时缓存自然销毁。
- Rust `BrandWorkspaceStore` 拥有 plan artifact、provider/resource/evidence snapshot、revision CAS、claim token、audit 和最终确认门。artifact 可由同一 BrandWorkspace 的其他已提交 Session 按 exact plan id 读取；创建 Session 只作 provenance。
- 超级媒介 capability 在本阶段仅允许分页读取资源池。Provider 不可用时 plan 明确进入 `unavailable`，候选为空；严禁生成 demo、随机或模板候选。

## Authority snapshot

开始计划时，Rust 重新派生并核对以下事实，不能信任 Renderer 传入的同名字段：

- industry 与 derived keywords 来自文章 operation 固定 knowledge version 的 `knowledge_version_facts`；
- articles 来自 exact article operation 的 approved revision；
- questions 来自已确认问题池的选中问题（confirmed-topic-plan 计划用其绑定的 `(pool_id, revision)`，direct 文章用同知识版本最新 confirmed 池）——被动路探测的输入；confirmed-topic-plan 文章通过 `source_plan_item_id` 与 plan item 的 `sourceQuestionIds` 精确映射到问题；direct 文章无可信映射时 `articleIds` 为空；
- question sources（被动证据）由 Node 在计划 start 时**对问题池逐问现场探测**产出（keyword-search typed port 的 `probeQuestion`：ARK Responses + `doubao_app.ai_search`，2-wide 窗口限流、逐问隔离失败）；prepare 校验 question id/question text 必须命中已确认池、URL 合法、id 唯一，`articleIds` 按权威映射重盖。**不依赖基线快照**（对齐 js_ai，2026-08-18 用户裁决）。
- 单篇与单次点数上限由 Rust 在 prepare 时直接读取本机 `config.json` 的 `distributionSpendLimits` 并冻结；Sidecar 请求中的同名值只可用于预估预算，不能成为或放宽计划事实。

prepare 后所有上述输入、Provider 非 secret 状态、候选资源白名单字段与四路证据都固定进 plan。start、edit、confirm 完成后均按 exact plan id 读回，不以 `latest` 猜测刚创建或刚修改的对象。

## 四路召回（对齐 js_ai ADR-0026/0031，2026-08-18 用户裁决完全对齐）

| 路              | 权重 | 证据来源                                                                                                                                                                                                                                                                                                                                                                                                               | 实现                                                       |
| --------------- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------ |
| 被动 passive    | 0.4  | 已确认问题逐问豆包探测的引用（域名/名称对齐资源池；多租户平台域名不算对齐，见下）                                                                                                                                                                                                                                                                                                                                      | `probeQuestion` 现场探测，2 QPS 双槽窗口；失败只降级不阻断 |
| 主动 active     | 0.2  | 全局单次召回（ADR-0031）：全部已批准文章 topics+行业+衍生关键词一次联网调用，产出 20–30 个真实渠道（注册域名门+反幻觉约束+topicNumbers→文章映射）                                                                                                                                                                                                                                                                      | `keywordSearch.search`（doubao-lite + enable_search）      |
| 保底 fallback   | 0.3  | 规则匹配：超级媒介结构化类目/官方 GEO 标记（`geo_platforms` 非空，2026-08-27 上线）命中（GEO 收录信号并入同一路说明）；召回层随机采样（垂类∩GEO 全量、纯垂类/单GEO 随机抽样，人群词不触发）见下方三轮裁决段                                                                                                                                                                                                                                                                                | 纯规则，无模型                                             |
| 偏好 preference | 0.1  | 人工策展：内置 exact 名单（DEFAULT_PREFERENCE_CHANNELS 十项；2026-08-27 用户裁决恢复 js_ai 原始预置名单——蓝色河畔/红安网/咸阳新闻网（GEO排名）、咸宁网主站、盐城网、南郡新闻/济南时报（官方头条号）、安庆都市网（可发GEO）（2026-08-28 用户指认修正：原「安庆新闻网」系名单录错的渠道名，全池从未存在，池内存活的是安庆都市网（可发GEO））、博客园、列举网（AI包收录）；未在候选快照出现的名字由计划运行的偏好路逐名命中验证）+ 品牌库 overlay（增补 `additional`/排除 `excluded`，`geo_channel_preferences` 单例表，management `/preferences/get | set`；overlay 目前无产品入口）                             | 域名优先；exact=精确名（全半角括号归一），用户条目=严格→模糊 |

匹配器与名单合成逻辑在 `src/shared/geo/channelRecall.ts`（js_ai sourceAlignment/preferenceChannels/globalRecall 的忠实移植：`strictMatchScore`、`fuzzyMatchScore`（品牌域名表+子串+去后缀+Jaccard）、`resolvePreferenceChannels`、`buildGlobalRecallPrompt`/`parseGlobalRecallResult`/`clampTopicNumbers`）。

**多租户平台域名豁免（2026-08-26 修复被动/主动误判；2026-08-27 扩名单）**：toutiao.com、douyin.com、kuaishou.com、xiaohongshu.com、bilibili.com、zhihu.com、weibo.com、qq.com、baijiahao.baidu.com、sohu.com、163.com、ifeng.com 上任意账号页/文章页/频道页共享同一注册域名，域名相等不构成「同一渠道」证据（此前头条号资源被错误对齐到全部头条文章引用与「今日头条美食垂类频道」全局召回）。2026-08-27 扩入门户系账号平台：sohu.com（搜狐号）/163.com（网易号）为用户确认的缺口，qq.com（企鹅号/公众号，覆盖原单列的 mp.weixin.qq.com）/ifeng.com（凤凰号）随同评估补入——账号内容挂在门户注册域名下，被动域名对齐与主动品牌兜底（搜狐/网易/腾讯/凤凰）两条误挂路径与头条系完全相同；代价是这些门户从「误挂」变「空窗」，账号级召回（标题作者后缀正式化/URL 模式解析/池内目录召回）另行落地。被动/主动路的域名对齐在这些平台上失效，只保留名称对齐；`isMultiTenantPlatformUrl` 为判定入口，契约字段 `channelRecall.alignment.multiTenantDomainExempt`。被动名称对齐用资源核心名（去掉尾部（官方头条号）类限定）做包含匹配。

**多租户来源的品牌家族兜底分豁免（2026-08-27 冲突检测修复）**：`fuzzyMatchScore` 的品牌分支此前以资源全名判定「属该品牌家族」，且渠道字零重叠时给 0.5 兜底分——限定后缀含品牌（如「白城融媒（今日头条）」）的无关账号被平台级推荐（toutiao.com URL → 品牌「今日头条」）成批误挂（实测 17/30 资源误命中）。修复：主动路对多租户来源传 `multiTenantPlatform: true`，品牌进入条件改用 `channelNameCore`（核心名），零重叠时兜底 0.5 → 0；偏好路用户手输条目保持默认品牌家族语义。修复后同一实测只剩 1/30 合理命中（同频道字重叠）。

**多租户名称匹配全分支核心名限定与两处语义修复（2026-08-28 用户裁决 Q1/Q10；全量池 24,770 条实测驱动）**：8-27 的品牌分支修复不完整——普通子串分支仍用全名比对，实测复现两条伪命中：来源「今日头条」(toutiao.com) vs「泾川融媒（今日头条）」=1.0（后缀里的平台词被子串分支当成名字证据）、「今日头条美食频道」vs 泾川融媒=0.4 恰好过线（`stripSuffixes` 只剥「网/频道」词表、不剥括号后缀，平台词混进 Jaccard 字符集交集）。修复：多租户来源的**全部**名称分支（子串/反子串/去后缀子串/Jaccard 两级）统一按核心名比对，括号后缀里的平台词永不构成名字证据；顺手修两处语义 BUG——域名分支改用注册域名（旧实现拿完整 URL 字符串与资源名比对）、品牌表（DOMAIN_TO_BRAND）只在 hostname 匹配（旧实现子串匹配整个 URL，路径含 `toutiao` 字样的第三方链接会误判品牌）。同轮：`sina.com.cn`/`sina.cn` 补入多租户表（k.sina.com.cn 看点号与头条号同构，全池挂载 105 条、platform=9 枚举佐证；新浪 L1 `article_{uid}_` 已支持，入表后账户级自动接管）；头条 L1 补数字形态 `/c/user/{数字}/`（全池 400 条资源、399 独立 id）；L2 尾缀解析加 SEO 关键词链防护——搜狐引用标题常带多段关键词链（`…_信息化_数据`、`…_平台_商家_餐饮`），旧实现取末段把「餐饮」「数据」当账号名造成伪账户级对齐（实测 16 条带尾缀引用 8 条是词链）；新规则多段链末段 ≥3 字保留（`…|消费面对面_佛山新闻`→「佛山新闻」）、≤2 字整条拒绝，实测垃圾 8/8 拦下、真账号 8/8 保留。被动 `nameMatches` 核心名兜底匹配同样加平台一致性门（Q6）：多租户引用要求引用平台族 ∈ 资源平台族，跨平台同名（全池 523 组，邯郸新闻网挂 5 平台）不再构成被动证据；非多租户引用（b2b168→八方资源网）保持无门。

**资源平台族第一信号=媒介盒子 platform 枚举（2026-08-28 用户裁决 Q7）**：自媒体资源 100% 携带 `platform` 枚举（文档附录「所属平台」；2,509 头条号/1,333 搜狐号/1,196 公众号/962 百家号…），此前解析进快照后从未消费。实测与注册域名一致率 99.30%（8,106/8,163，真矛盾 32 条为转售商录错，如 platform=新浪挂 weibo 链接）。`resourcePlatformFamilies` 升级为 枚举 ∪ 域名 ∪ 名称后缀别名 三源并集（一致性门用并集，录错数据的 fail-safe 放宽方向）；另设 `primaryPlatformFamily`（枚举→域名→后缀，单值）供代表比较器的官网优先判定（Q15 后家族键不再消费它）与官方型判定这类需要确定性单值的场景。22=垂类杂项（值得买/抖音电商/淘宝/携程混装）不映射，族交给域名推导。

**平台官方型通道（2026-08-28 用户裁决 Q3a）**：多租户平台级来源（「搜狐美食频道」「今日头条美食频道」等）此前要么成批误挂账号渠道（旧 BUG）、要么修复后颗粒无收。数据实证：sohu.com 根路径挂载 981 条中仅 21 条核心名含「搜狐」（搜狐网历史（GEO）/搜狐网三农（GEO）…真·门户官方频道转售），其余 960 条是转售商懒得填账号链接的无名账号——**根路径单独不构成官方判定，品牌核心名条件缺一不可**（双条件全池 ~89 条：搜狐 21/网易 25/新浪 12/头条 12/凤凰 12/腾讯 7/百度 0）。主动路新增官方型通道：来源为多租户平台级（域名族 F）∧ 资源 `platformOfficialFamily`=F（entrance 根路径 ∧ 域名族 F ∧ 核心名含 F 品牌词）即命中，证据 label 标注「命中平台官方型资源」；账号渠道永不承接平台级信号。主动召回 prompt 同轮收紧（Q4）：平台级泛渠道（知乎/小红书/公众号这类无具体频道页的整站条目）最多 5 条且必须给具体频道页 URL，优先垂媒官网/具体账号/门户具体频道页。官方型只进主动路；被动侧保持关闭（sohu 引用几乎必是 `/a/{id}_{mpId}` 账号文章，门户编辑内容无法从 URL 分辨）。

**变体家族/包两级塌缩（2026-08-28 用户裁决 Q13/Q15；池内同渠道重复挂牌的治理）**：池内同一渠道常被重复挂牌或多套餐（列举网 7 变体、蓝色河畔 5 变体、博客园 6 变体——名称后缀是转售商随手写的规格词），大渠道下另有真实子频道（学习强国 92 条：（江南时报）/（江苏经济报）/（电力税务）…括号是机构/频道身份）。两级结构：**家族**=核心名（`channelNameCoreAll`，连【】尾块一起剥）主干的包含连通聚类（Q15 修订：`北京列举网 ⊃ 列举网`、`列举网geo ⊃ 列举网` 即同主干，含「长度差 ≥2」门槛防数字序号假阳性，家族键取连通分量中最短核心名即最泛主干；跨平台同名（蓝色河畔（今日头条）与蓝色河畔官网）并入同族——原「核心名+主平台族」分家口径会把 geo 词干/城市前缀/连字符段裂成多个主干，列举网系 7 席由此塌到 ≤2 席）；**包**=规格词尾块或无尾块 → 默认包（①重复挂牌+②套餐规格，同渠道同产品），身份词尾块按尾块值分子频道包（③学习强国型）。规格词判定**数据驱动不维护词表**：某尾块出现在 ≥10 个不同核心名上即通用规格词（实测可发GEO 843/GEO 167/包收录 108/官方 131；江南时报/湖南发布等身份词恒为 1；漏判如「带图」(4 核心) 的失败方向是自成一组多占一位，不会错误合并）。同（家族,包）只出 1 个代表进推荐走查，代表比较器=`非junk → 官网（自有域名）优先 → 证据权重 → geo_platforms 多 → 价格低 → 基础名（核心名短）→ id 小`（Q15 修订：官网优先兑现「必有官网」——蓝色河畔这类头条+官网渠道以官网版为家族代表；基础名优先让「餐饮界首发」类主干变体让位给「餐饮界」）；家族 ≤2 席（`variantFamily.familyQuota`）后按全局排序进媒体/自媒体配额；被塌缩/限席挤出的同胞留在对齐池不算失败。展示四路统一按家族折叠（chip ×N，被动对齐行带 `同名变体 ×N · ¥min-max`，`passiveAlignedChannels` 按家族计数计入 50 上限；`summary.alignedFamilies` 新增家族口径与 `alignedResources` listing 口径并存）。`discoverySummary`/`preferenceMatchedChannels` 与候选 `variantFamily` 字段随 `discovery/finish` 落投影，Rust 透传同旧模式。

**随机号商品压制（2026-08-28 用户裁决 Q11）**：名称含 `随机|打包|千粉|百粉|水军|套餐N家|N家媒体` 的转售商品（全池 211 条：「知乎百粉打包5条」「百家号（账号随机）」「80家媒体打包」；`包收录` 是正规服务特性不算，华尔街财经（包收录）类 123 条不受影响）真实可买，**不剔除、可对齐可展示**，但三处压制：候选排序链在分档后加 junk 位（靠后）；包/家族代表比较器 junk 位最前（全组皆 junk 才轮到）；面板正常显示（原名自带「随机/打包」字样用户可见）。

**偏好匹配核心名化与配额前命中清单（2026-08-28 用户裁决 Q12）**：内置名单逐字 exact 匹配被池子命名漂移静默打断（名单 10 项死 2 项：列举网（AI包收录）已不存在但列举网有 7 个存活变体、安庆新闻网仅有安庆都市网）——exact 语义改为**核心名相等**（两侧 `channelNameCoreAll` 归一；用户 overlay 的 domain 匹配不变）。更深一层：旧面板「匹配渠道」从推荐集反推，而偏好单路 0.15 永远进不了 top30 → 恒显 0（上次实测 8/10 匹配成功仍显示 0）。新增 `preferenceMatchedChannels`（配额前逐名单项一行：matched/代表名/价格/variantCount/recommended，代表=全名逐字命中者优先否则包代表规则；未命中项 matched=false 如实展示）随投影落库，面板偏好块按行渲染（✓=进入推荐）。偏好名单 2026-08-28 修正一项：原「安庆新闻网」为名单录错的渠道名（全池从未存在，池内存活的是「安庆都市网（可发GEO）」¥8），按用户指名改为后者——修正后十项全部池内存活（列举网（AI包收录）全名虽不存在，核心名匹配命中 7 个变体）。

**主动路名称匹配收严（2026-08-28 用户裁决：「只要真正正确的渠道」）**：主动路名称回落从 `fuzzyMatchScore≥0.4` 换成专用 `activeNameMatchScore≥0.8`——**Jaccard 字符交集档整体退出主动路**（残留误配实测：中国团餐网→中国妈妈网 0.5、今日头条美食频道→美妆头条 0.4、腾讯新闻美食频道→新闻快讯网 0.4——字符有交集但完全不同机构），只认：包含关系（1.0/0.8 子串与去后缀子串）、**共享前缀 ≥4 字**（新增规则：界面新闻主站↔界面新闻消费板块 共享「界面新闻」是真匹配，Jaccard 弃用后由前缀接住；对照中国团餐网/中国妈妈网 仅共享「中国」2 字不命中）、品牌分支强重叠（≥0.8）与非多租户品牌兜底 0.5（36氪官网→36氪（百家号）这类跨平台官方账号；多租户平台级兜底恒 0 不变）。域名相等与平台官方型通道不变。真实数据重放（旧计划 27 来源 × 全量池）：红餐网→红秀网/红网、职业餐饮网→餐饮行业网、中国团餐网→中国妈妈网/中国美网、界面新闻→新闻快讯消费/消费新闻网 等跨机构误配全部清零；餐饮界（域名）、亿欧网（域名）、南方都市网、界面新闻主站（前缀）、中国餐饮品牌网（前缀）等真匹配保留。`fuzzyMatchScore`（含 Jaccard）保留给偏好路用户手输条目（人工输入需要容错）与其它调用方。契约 `activeNameMatching` 同步。

**URL 字段参与匹配：case_link 第二域名信号（2026-08-28 用户裁决）**：池侧域名信号从 entrance_link 单字段扩为 cleanResourceDomains（entrance + case_link 的注册域名，多租户域剔除），主动域名命中、被动域名对齐、池反查（buildPoolDomainNameMap）三处统一换用。依据：case_link（收录案例链接）全池 100% 有值、98.3% 与 entrance 同域（16,844/17,137，证明它是渠道自己域上的案例页）、16,107 条独立域；**7,599 条资源 entrance 为空时 case_link 是唯一域名信号**（八方资源网型——实测该渠道被动对齐从标题兜底升级为域名对齐，池反查命中率 0→1）。真实反查：亿欧/南方都市报/羊城晚报/界面/36氪 的召回来源域名经 case_link 各多命中数条真渠道；红餐网 canyin88.com 池内 entrance/case 均无（该渠道确实不在池）。资源快照新增可选 caseLink 字段。
**URL 匹配防误判：域名歧义防护（2026-08-28 用户裁决）**：URL 匹配的两个实测风险——同域多频道（子频道/主站偏差）与转售商挂错链——以「唯一域名集合」防护：域名下所有核心名能经包含关系（A⊃B）传递连通 = 唯一域（全池干净域名 7,135 个中 6,131 个，86%；canyinj.com 的 餐饮界/餐饮界首发、b2b168.com 的 八方资源网），域名命中直接放行；连不到一起 = 歧义域（1,004 个，两类：ppwll.cn 式跨机构聚合域——同时挂 中国商业报道网/广佛都市网/每日快报/邯郸在线 四家无关机构，是真正的毒药；ycwb.com 式同机构多产品域——39 个羊城晚报系挂牌），域名命中必须名称佐证（主动路 activeNameMatchScore≥0.8、被动路 Q6 门控 nameMatches）。代价与取舍：歧义域上无名称佐证的真匹配会被放弃（金羊网评论 不含「羊城晚报」字样），宁缺毋滥；case↔entrance 域名冲突的 219 条资源天然被歧义判定覆盖（两侧域名各自参与连通性计算）。

**类目映射按官方附录命名重构（2026-08-28 用户裁决；附录经代理商门户登录后从官方文档逐条抄录）**：废除 js_ai 移植的两张「行业词→码」百条硬映射表（餐饮→2 的错误正源于此），改为**官方枚举为单一事实源**——`MEDIA_CHANNEL_TYPE_NAMES`（附录：频道类型，码→官方名：1 IT科技/2 生活消费/…/18 食品餐饮/19 工业贸易…）与 `WE_MEDIA_INDUSTRY_NAMES`（附录：行业分类：1 文化/…/13 美食/22 搞笑/25 生活…）。行业匹配 `industryCodesFor`：整串包含 ∨ 别名碎片（仅词面互不包含的词进 `INDUSTRY_TERM_ALIASES` 小表，如 医美→健康/医疗、餐饮→美食/食品）∨ 共享 ≥2 字子串（中文连写词「汽车改装」×「汽车网站」共享「汽车」），**一个行业命中类目码集合**（多码）。营销专区按**类目名**排除（套餐系列/最新秒杀/十元专区/其他——码在两表含义不同：媒体 13=套餐系列该排除、自媒体 13=美食是核心类目，按码排除会误杀）。官方附录同轮抄录的还有：所属平台（与我们实证破译的 24 项完全一致，UC头条=大鱼号）、综合门户（31 项）、所属地区、资源/订单状态——暂未消费，留档。证据 label 用官方类目名（「超级媒介结构化类目匹配「食品餐饮」」）。
**类目映射全量校准 + 垂类保底配额（2026-08-28 用户裁决 Q16/Q16b）**：全池逐码采样读判完成两表校准——媒体 `channel_type`：**码 18=食品餐饮**（237 条 84% 垂媒；旧映射 餐饮/美食→2 是 js_ai 移植错误，码 2 实为生活消费，581 条中食品名仅 2.8%），补 物流→19、企业/品牌→14；码 13=媒体套餐打包（不映射任何行业）、码 0/100=未分类杂项。自媒体 `industry_category` 原表 18 项实证全对，补缺：生活种草=25、三农=3、公益=15、动漫=17、情感=21、职场电商=20、商业新闻=22。**垂类保底配额**（`recommendation.verticalQuota: 10`）：垂类命中（行业类目 ∨ 行业词名命中——人群词不算，防配额被半相关站占满）的候选在媒体/自媒体配额走查中先行占位，推荐 30 中至少 10 席，不足回流整体排序；实测中华餐饮网/餐饮头条类次级垂媒由此进入推荐。（该占位配额经同日二轮随机保留席、三轮召回层采样两度推翻，现行契约为 `fallbackRecall`、无 `recommendation.verticalQuota` 字段——见下段）同轮修复：主干包含连通加「长度差 ≥2」门槛（泛站观察2 ⊂ 泛站观察27 的数字序号假阳性）；mergeGeoChannelPathHits 分数累加 toFixed(10) 归一。
**保底召回随机采样：三轮裁决（2026-08-28，推翻同日二轮的保留席前置）**：随机性从「选席」下沉回「召回」——保底路召回输出 = 垂类∩GEO（t0）**确定性全量** + 纯垂类（t1）每次 Fisher-Yates **随机补足 26 席**（契约 `fallbackRecall.verticalQuota`）+ 单 GEO（t2）随机补足至 **30 封顶**（`fallbackRecall.totalCap`）。只有抽中的渠道挂 fallback 证据（0.3）；未抽中即本次未被保底路召回、不带 0.3 权重（多路命中者未抽中时权重回落，如被动+垂类→0.4，与主动路未召回同理）；人群词（t3）不再触发保底证据。**合并层恢复纯加权排序**：四路并集 → 权重 → 命中路数 → 分档 tie-break → junk → 垂类名 → 被动覆盖 → 名称，取前 30，无任何保底占位/前置（二轮的保留席前置被移除——池充足时 30 席被随机序占满、权重排序在最终列表上失效，多路高权重渠道被随机挤出且列表显示乱序）。动因（数据飞轮）：t0 已知优质不需探索；t1 每次随机抽不同子集，跨运行统计哪些垂类渠道被选中/被引用，挖掘隐藏优质垂类（二轮动因保留记录：tier 主序+拼音序 tie-break 实测产生《城市快报》《今日X》系固定霸榜，改随机「不是总是固定几个」）。随机源可注入（`buildDistributionCandidates` 的 `random` 参数，缺省 `Math.random`，测试定苗可复现）；面板保底路匹配渠道按三档分组展示不变（垂类∩GEO / 垂类 / GEO 标记，档位从证据 label 推导；t3 无证据自然不再出现）。`alignedByPath.fallback` 口径随之变为真实召回数（t0 全量 + t1/t2 抽样，常态 ≤30；t0 总量超过 totalCap 时不截断、可 >30），不再统计全量规则命中。
**候选排序链与抖音边界（2026-08-28 用户裁决 Q5/Q14）**：同权重 tie-break 从拼音序改为「分档 → junk → 垂类名命中（名称含行业/人群词，兑现子频道垂类优先并补结构化类目覆盖不到的缺口）→ 被动覆盖问题数 → 被动引用数 → 名称」。抖音被动账户级对齐为数据源物理边界：引用是 `iesdouyin.com/share/video/{视频id}`（非账号 id）且标题 hashtag 风格无尾缀，L1/L2/L3 均无法定位账号（13/50 引用实测）——接受并如实展示（平台级分组、无账号标注）。

**被动来源按问题配额与跨问排序（2026-08-27 用户裁决；同日二轮裁决修订）**：被动探测不再按总量 100 顺序截断（旧实现前 5 问即占满名额，后续问题引用全丢）。`selectPassiveSources`（shared 纯函数）：每问最多 10 条引用（`perQuestionCap`），按「渠道=引用 URL 注册域名出现在多少个不同问题」降序、同渠道引用数降序、原始出现序排。**二轮裁决（同日）：总量帽 `totalCap=50` 废除，排序只决定展示顺序、全量返回所有探测过问题的引用**（探测问题上限 20 保持，成本护栏）；契约 `passiveRecall` 同步为 `alignedChannelCap: 50`（见下）。同轮修复：question+url 去重此前写的是 `!seen.add(...)`——`Set.add` 恒返回集合本身，判重从未生效，旧实现靠总量帽把重复引用截掉才未被察觉，现已改为 `has/add` 分离。

**被动对齐渠道列表与三分口径（2026-08-27 用户裁决二轮）**：面板四路展示从「来源 N · 匹配渠道 M」改为三分「来源 N · 对齐渠道 A · 进入推荐 M」——对齐（拿到某路证据）与推荐（经权重排序进入 30 候选）是两件事，对齐而未推荐不是失败（此前"匹配渠道 0"的恐慌正是把两者压成一个数的误导；权重加法下被动单路 0.4 恒输给 active+fallback 0.45 的组合，属用户裁决保留的算术）。被动路对齐渠道列表上限 50（`alignedChannelCap`，按跨问覆盖>引用数排序），随 `discovery/finish` 以 `passiveAlignedChannels`（resourceId/kind/name/价格/citations/questions/accounts/recommended）落进投影；`discoverySummary` 扩展 `alignedByPath`（推荐截断前按路分桶对齐计数）与 `citationDomains`/`citationDomainPoolHits`（池反查命中率分子分母）。全局推荐上限 30、媒体/自媒体配额 20/10、四路权重加法均不变。

**账户级对齐（2026-08-27 用户裁决：三层解析，全部第一方，不依赖供应商账号字段）**：多租户引用对齐到具体账号的证据分层——L1 URL 内嵌账号标识（搜狐 `/a/{id}_{mpId}` 的 mp_id、新浪 `article_{uid}_`、百家号 `app_id`、头条账号页 `/c/user/token/{token}`、公众号 `__biz`；引用与资源两侧同键相等即命中，证据 label 带「账号：搜狐号#122878478」式标注）> L2 引用标题尾缀账号名（`xxx_账号名`，豆包标注无作者字段，头条引用实测 9/9 带）× 渠道核心名（`channelNameCore` 剥平台括号）+ **平台一致性门**（引用域名平台族 ↔ 资源名称括号别名/entranceLink 域名，跨平台同名不构成账户级证据）> L3 引用页面作者解析（server 抓页提取 JSON-LD `author`/`article:author` meta，`resolvedAccountName` 回填随 prepare 落库；≤20 条/计划、单条 8s 超时、按 URL 去重、失败静默降级到平台名；复用材料导入的 undici+SSRF 守卫）。注册域名相等永远不构成账户身份（多租户门不变；无门的 `nameMatches` 核心名路径保留——济南时报先例）。契约 `accountResolution` 同步；`iesdouyin.com`（抖音分享域，独立注册域名）补入多租户表。

**引用站点显示名与池反查（2026-08-27 用户裁决）**：组名优先级=豆包 site_name > **池反查**（超级媒介资源 entranceLink 域名 → 渠道核心名的动态映射，随发现时构建；多租户域名与核心名冲突域名不进映射，宁缺毋滥）> 品牌表 > 标题尾缀站点名（` - 八方资源网` 式短尾缀，仅前两级 miss 时）> 裸域名。映射结果以 `citationSiteNames`（注册域名 → 展示名）随 `discovery/finish` 落投影，面板与 Rust 透传同 `activeRecallSources` 模式（旧 Sidecar 缺省保持原状）。契约 `citationDisplayNameChain` 同步。

**召回输入快照与四路复盘展示（2026-08-26；2026-08-27 二轮扩展）**：`discovery/finish` 随发现结果把召回输入快照落进投影——`activeRecallSources`（主动路全局召回原始渠道 title/url/articleIds/reason，reason=LLM 推荐理由原始回答关键信息 ≤200 字）与 `preferenceChannelNames`（偏好路生效名单名字快照）；被动路来源本就在投影 `questionSources`（新增 `resolvedAccountName` 可选字段），保底路无外部输入（规则输入=行业/人群）。evidence.reference 对 passive/active/preference 逗号合并（Rust 确认门前缀校验兼容；fallback 保持 `industry:`/`audience:`/`geo:` 原样不合并）。右侧「渠道发现与分发计划」面板在发现完成后（discovering 之外的状态）渲染四路复盘，每路分「召回来源」与「对齐/匹配渠道（资源池命中）」两组：被动来源按渠道（注册域名）分组聚合（组名链见上段；组内账号后缀或 L3 解析名一致时附带（如「今日头条 · 深氪新消费」）；组头带 `N 条引用 · 覆盖 M 个问题 · 域名`，跨问覆盖多的渠道在前；组内每条引用是 `{问题，标题（url 链接）}`；命中的组整组高亮），主动来源行携带 LLM 推荐理由；被动路匹配渠道区渲染 `passiveAlignedChannels`（✓=进入推荐 accent 高亮，其余中性样式并明示「被推荐配额挤出而非对齐失败」，chip 附账号与引用/覆盖计数；旧计划无该字段回落到推荐集内被动证据），其余三路匹配渠道 chips（accent 高亮）；来源↔渠道经 evidence.reference 关联；纯只读，不与聊天确认卡片的候选勾选职责重叠。偏好路标题旁明示「内置验证名单」（`geo_channel_preferences` overlay 无产品入口，生效名单=内置十项；学习式偏好为演进方向另票处理）。

**官方 GEO 标记触发与垂类分档（2026-08-27 用户裁决，超级媒介 `geo_platforms` 字段上线当日接入）**：资源快照新增 `geoPlatforms`（官方标记的 AI 收录平台标签，去重保序；全池实测媒体 15960 中 462、自媒体 8789 中 366 个非空，枚举 DeepSeek/豆包/通义千问/腾讯元宝/文心一言/Kimi/其他）。保底路触发条件从「行业类目 ∨ 人群词」扩为「∨ 官方 GEO 标记」——`geo_platforms` 非空是结构化数据可独立触发；备注关键词命中（`geoIncluded` 旧逻辑，官方字段优先、关键词兜底）仍只是信号文本不独立触发。动机之一：GEO 命中资源中 52 个媒体+23 个自媒体类目码为 0（未分类，文档枚举外），行业匹配永远筛不到它们。候选排序与文章分配在「同权重同命中数」上新增分档 tie-break（`fallbackPreferenceTier`）：垂类∩官方GEO > 纯垂类 > 纯官方GEO > 仅人群词；四路按路径累加的权重语义不变。geo 触发的 fallback 证据 reference 为 `geo:平台1/平台2`（与快照 geoPlatforms 逐字一致），Rust 确认门按此交叉校验。

## 推荐、过滤与不确定性

四路权重固定为 passive `0.4`、active `0.2`、fallback `0.3`、preference `0.1`（2026-08-28 用户裁决自 `0.4/0.3/0.15/0.15` 再调整——保底升至 0.3 让垂类规则路在权重算术上可与主动路抗衡，主动/偏好下调），同一资源只累加不同路径。资源必须先满足超级媒介 `status=2`；数值价格先按统一公式换算为点数（`价格 × 1.6 × 10`，以分为基向上取整），超过用户“每篇文章最高点数”的资源在对齐与配额前硬过滤。该设置默认 `3000` 点；“每次分发最高点数”默认 `20000` 点。两项由左下角「个人信息」维护，并在新建计划时冻结，后续修改不反写已有计划。自动分配按候选顺序逐篇选择未使用且可纳入剩余总点数的渠道；昂贵候选放不下时继续尝试下一候选。价格未知无法证明在单篇上限内，因此不返回、不自动分配；旧计划若已有未知价渠道，确认门仍会阻断。计划预算可以低于用户总上限，但不能放宽该硬上限；确认与发布预览都会重新校验单篇和总点数。**发布率不是决策输入（用户裁决 2026-08-18）**：不设最低发布率门槛，发布率 `0`/null/未知既不过滤、不进不确定性、不阻断确认，也不在候选行展示；调度器消费已确认计划时快照缺失记 `0`，不因发布率报 `publish_channel_rate_unknown`。**被动证据为空不阻断**（对齐 js_ai：探测失败只是没有被动路，`question-source-evidence-missing` 阻断码已删除）。

候选的 name、price 和 availability 只能来自 typed Provider resource snapshot。候选展示来源证据、召回路命中（被动/主动/保底/偏好，含各路证据说明）、适配理由、价格与权重；不再逐条展示风险行（用户裁决 2026-08-18），价格未知仍由行内「未知」标记与确认阻断码呈现。空并集保持空，不移植 `random_balanced`。Rust finish/confirm 门的证据校验按四路新契约：passive 的 reference=questionId 且 URL 必须命中来源、articleIds 等于来源并集；active 的 `recall:` 前缀；fallback 的 `industry:`/`audience:`/`geo:`（geo 载荷须与候选快照 geoPlatforms 逐字一致）；preference 的 `preference:` 前缀。

一篇文章只分配一个渠道且渠道不能复用。计划支持 1:1 或媒体/自媒体比例映射、渠道组合、预算和发布时间编辑。Rust 确认时重新计算选择、分配、价格/成功率、证据、预算和时间护栏；仅靠客户端清空 `blockingIssues` 不能越过确认门。

确认提交成功后，`distribution-plans/confirm` 路由在同一 Session 投递纯隐藏 `XIAOJING_DISTRIBUTION_PLAN_DECISION` reminder（只携带 plan/operation identity、revision、status 与 assignment 数），唤醒 agent 从权威计划继续进入发布准备，不重复询问已确认计划。提醒入队失败不回滚已提交的确认，响应显式返回 notification 状态。

`plan_distribution` 工具结果是聊天转录的一部分，只返回**卡片最小投影**（契约类型 `DistributionPlanCardProjection`，`shared/geo/distributionPlan.ts`：id/状态/预算/选择/阻断 + 候选的名称·报价·路径命中·适配·证据标签≤64 字；articles 只带 id、assignments 全量保留防轮询前确认）——约 13K 字符/6K tokens，完整权威投影由卡片 3s 轮询 `/distribution-plans/latest` 水合。转录里的费用字段一律是点数（`budgetPoints` / `estimatedPricePoints`，由服务端 `cnyToPoints` 算好）：CNY 金额与换算倍率不进聊天，agent 只能引用点数字段复述费用；预算入参同样只收点数（`budgetPoints`，服务端 `planDistributionBudgetCny` 换算，缺省使用当前“每次分发最高点数”），`publishStartAt` 缺省取当前时间——确认即发：用户授权启动后到期项立即被调度器认领执行，只有显式传入未来时间戳才是定时发布（用户裁决 2026-08-26，取代旧的 +1h 默认）；确认卡在轮询水合前提交时按 `pointsToCny` 把点数预算回算为内部 CNY（预算上限语义，任意点数精确往返）。

## 聊天修订（票 38，ADR 0003）

待确认（draft）计划的渠道选择与计划参数可经通用闸门修订工具 `revise_gate_content`（gate=`distribution-plan`）改/删/增：`subject='channel'` 的 add/delete 等价于选择/取消选择渠道（取消选择同时把对应分配置为 `unassigned`），`subject='assignment'` 修改逐篇分配（渠道必须已选择），缺省 `subject` 修改预算/发布开始时间——预算补丁只携带点数（`budgetPoints`，服务端按 `pointsToCny` 换算回内部 CNY，兼容旧转录的 `budgetCny`）。handler 复用既有 `DistributionPlanningService.edit`（白名单字段整组替换、`applyDistributionPlanEdit` 重算 blockingIssues），`edit` 携带 `reason`（用户指令原文）落 `geo_distribution_plan_audit.reason` 审计列。confirmed/discovering 计划分别按 `target_not_pending` 拒绝；确认卡（`DistributionGateCard`）待决期间每 3s 轮询 `/distribution-plans/latest`，服务端渠道选择变化时采信服务端（服务端胜），否则保留本地勾选。

## 测试边界

默认 unit/DOM/Rust tests 使用 mock capability 与临时 BrandWorkspace，不访问网络、凭据或真实用户目录。资源 TTL 通过注入 `now()` 确定性验证。credentialed smoke 不属于默认验收；即使显式运行，也只能读取资源目录，不能下单或发布。路由接缝回归（`src/server/__tests__/xiaojing-decision-receipt-gates.integration.test.ts`）断言确认后注入决策回执信封、响应携带 notification 状态、投递失败不影响决策成功。
